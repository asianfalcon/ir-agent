[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "alphasonar"
version = "0.1.0"
description = "AlphaSonar: AI-powered alpha detection, tracking, forecasting, and feedback system."
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
  "akshare>=1.18",
  "anthropic>=0.70",
  "apscheduler>=3.11,<4",
  "beautifulsoup4>=4.13",
  "curl-cffi>=0.13",
  "kuzu>=0.11,<0.12",
  "lancedb>=0.25",
  "langchain-text-splitters>=1.0",
  "lxml>=5",
  # server.py uses the MCP 1.x low-level decorator API.
  "mcp>=1.20,<2",
  "numpy>=2",
  "pymupdf>=1.26",
  "sentence-transformers>=5",
  "starlette>=0.50,<2",
  "tushare>=1.4",
  "uvicorn>=0.38,<1",
  "watchdog>=6",
  "yfinance>=1",
]

[project.optional-dependencies]
reports = [
  "pillow>=11",
  "python-docx>=1.2",
  "reportlab>=4.4",
]
ocr = [
  "pdf2image>=1.17",
  "pytesseract>=0.3.13",
]
dev = [
  "pytest>=8",
  "ruff>=0.12",
]

[project.scripts]
# Short user-facing commands.
alpha-mcp = "alphasonar.interfaces.mcp.server:run"
alpha-server = "alphasonar.interfaces.mcp.http_server:run"
alpha-init = "alphasonar.storage.db_initializer:init_schema"
alpha-track = "alphasonar.connectors.watchdog_monitor:start"
# Explicit and former names remain compatibility aliases.
alpha-hound = "alphasonar.connectors.watchdog_monitor:start"
alphasonar-mcp = "alphasonar.interfaces.mcp.server:run"
alphasonar-mcp-http = "alphasonar.interfaces.mcp.http_server:run"
alphasonar-init-db = "alphasonar.storage.db_initializer:init_schema"
alphasonar-watch = "alphasonar.connectors.watchdog_monitor:start"
alphasonar-hound = "alphasonar.connectors.watchdog_monitor:start"
ira-mcp = "alphasonar.interfaces.mcp.server:run"
ira-mcp-http = "alphasonar.interfaces.mcp.http_server:run"
ira-init-db = "alphasonar.storage.db_initializer:init_schema"
ira-watch = "alphasonar.connectors.watchdog_monitor:start"

[tool.setuptools.packages.find]
where = ["src"]
include = ["alphasonar*", "ira"]
namespaces = true

[tool.setuptools.data-files]
"share/alphasonar/prompts" = ["resources/prompts/*.md"]
"share/alphasonar/policies" = ["resources/policies/*.md"]
"share/alphasonar/schemas" = ["resources/schemas/*.json"]
"share/alphasonar/dictionaries" = ["resources/dictionaries/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]

[tool.ruff]
target-version = "py311"
line-length = 110
